# 값을 채워 .env.local 로 복사해 씁니다. .env.local 은 커밋하지 않습니다.

# Anthropic 비전 분석
ANTHROPIC_API_KEY=
# AI 초안을 실제로 부르지 않고 고정 더미를 돌려줌. 크레딧 없이 화면을 볼 때만 씀
# 1 정상 초안, not-animal 동물 없음 판정, low-quality 어두운 사진 판정, 0 이나 없으면 실제 호출
ANALYZE_MOCK=0

# 개발 전용. 크레딧 없이 실제 모델 응답을 볼 때만 gemini 로 둠
# 무료 티어는 입력과 출력이 구글 학습과 사람 검토에 쓰여 실제 제보 사진을 보내지 않음
# 비우면 Anthropic 으로 감. 크레딧 충전 뒤 이 두 줄과 vision/gemini.ts 를 지움
ANALYZE_PROVIDER=
GEMINI_API_KEY=

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# SNS 로그인. 브라우저로 나가야 하므로 NEXT_PUBLIC 접두사가 필요함
# anon 키는 공개 키이고 테이블 접근은 RLS 와 REVOKE 로 막혀 있음
# 비워 두면 로그인 벽을 세우지 않고 익명 경로만 동작함
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 카카오 로컬 API 역지오코딩과 장소 검색. 서버 전용
KAKAO_REST_API_KEY=

# 운영 API 공유 토큰. x-admin-token 헤더로 보냄
# 비워 두면 운영 API 가 모든 요청을 거절함
ADMIN_API_TOKEN=

# e2e 실행용 레이트리밋 완화. 로컬과 CI 에서만 true 로 둠
RATE_LIMIT_RELAXED=

# 배포 도메인. OG 메타와 QR 코드 생성에 사용
NEXT_PUBLIC_SITE_URL=http://localhost:3000
