# 이동 경로 추적과 다음 목격 예측

노션 옮기기 대기 중인 임시 보관본입니다. 워크스페이스가 무료 블록 1,000개 상한에 걸려 페이지를 만들지 못했고, 자리가 생기면 `01 제품 기획` 하위 페이지로 옮기고 이 파일을 지웁니다. 문서 단일 원천은 노션입니다.

흩어진 목격 제보를 시간순으로 이어 동물의 이동 경로를 만들고, 다음 목격 가능 지역과 탐색 순서를 내는 기능입니다. 한 번의 목격을 등록하는 서비스와 갈리는 지점이고, AI 활용도 심사에 대한 답이 여기 있습니다.

## 왜 이 기능인가

게시판은 이미 포인핸드와 당근이 하고 있습니다. 다시집이 가질 수 있는 것은 게시글이 아니라 목격 데이터의 연결입니다. 사진 한 장으로 글을 대신 써 주는 것은 사람이 조금 더 시간을 쓰면 되는 일이지만, 흩어진 목격 다섯 건에서 이동 방향을 읽어 다음에 나타날 곳을 좁히는 것은 사람이 눈으로 하기 어렵습니다. 인간의 개인 지성이나 집단지성으로 추정하기 어려운 자리에만 AI를 씁니다.

## 공식

다섯 줄 전부 결정식입니다. 모델이 점수를 만들지 않고 계산된 값을 해석해 행동 문장으로 바꿉니다.

```
T1  연결 가능성 - 두 목격을 한 개체의 연속으로 볼 수 있는가
    feasible(A→B) = d(A,B) / min(v_max × Δt, D_MAX) ≤ 1
    v_max = 소형 3.0 · 중형 4.5 · 대형 6.0 km/h
    D_MAX = 소형 5 · 중형 10 · 대형 15 km

T2  경로 신뢰도 - 점수는 노드 최소값, 이동성은 다리 평균값
    track = min(match_i) × (1 − mean(d_i / min(v_max × Δt_i, D_MAX)))

T3  방향성 - 이동생태학의 straightness index, 다리 수로 수축
    κ = |Σ v⃗_i| / Σ|v⃗_i|      0 = 제자리 맴돎, 1 = 한 방향 직진
    κ_eff = κ × n_leg / (n_leg + 1)

T4  다음 목격 예측 - 중심은 방향으로 밀고, 원은 √t 로 퍼짐
    중심 C = P_last + κ_eff × v_eff × min(h, H_DRIFT) × û
    û = Σ v⃗_i / |Σ v⃗_i|   마지막 다리가 아니라 다리 합벡터의 단위벡터
    H_DRIFT = 8h            이후로는 중심이 더 밀리지 않음
    반경 r = σ × √h + ε_gps,   0.4km ≤ r ≤ 8km
    σ = 소형 0.28 · 중형 0.38 · 대형 0.50 km/√h

T5  탐색 우선순위 - 예측 원의 경계에 가까운 지점이 앞
    priority(P) = w_type / (1 + |dist(C,P) − r| / r)
    w_type = 하천·공원 1.0 · 산책로 0.9 · 학교·아파트 0.7
```

한 문장으로 줄이면 방향이 일관될수록 예측 원의 중심이 앞으로 가고, 시간이 지날수록 원이 √t 로 커집니다. 순수 확산이면 변위가 √t 에 비례하고 방향성이 있으면 선형 성분이 더해지는 랜덤워크라, 숫자 하나하나를 심사위원이 검증할 수 있습니다.

## AI가 맡는 자리

```
단계             담당              산출
체인 후보 생성    결정식 T1·T2      시간순 목격 열
같은 개체 판정    모델 + 사진 4장    photoConsistency (consistent·mixed·unclear)
원 그리기        결정식 T3·T4      중심 좌표와 반경
어디부터 찾나     모델 + Kakao POI  탐색 순서 3곳과 주의 한 줄
```

모델은 개체 동일성을 확정하지 않습니다. 품종을 단정하지 않고 점수를 다시 매기지 않으며, 프롬프트에 좌표 숫자를 넣지 않고 지역명과 방향 낱말로만 말합니다. 사진은 털색·크기·목줄이 서로 어긋나는지만 보고 같은 개체라고 말하지 않습니다.

## 설계 상수

```
MIN_LEG_SCORE           45            경로에 넣을 매칭 점수 하한
MIN_LEG_SIMILARITY      0.82          외형 벡터만으로 경로에 들일 하한, 표본으로 못 정해 기본값 유지
MIN_TRACK_NODES         2             경로가 성립할 최소 목격 수
MAX_TRACK_NODES         12            최근 것부터 자르는 상한, 오래된 다리는 예측에 덜 쓰임
D_MAX_KM                5 / 10 / 15   체급별 이동 거리 상한 추정, unknown 은 10. 공백이 길어도 상한이 무한히 늘지 않게 둠
H_DRIFT_HOURS           8             방향 지속 시간 추정, 이 시간을 넘으면 표류가 포화
PHOTO_MIXED_FACTOR      0.6           사진이 mixed 일 때만 신뢰도를 깎는 감쇠 계수 추정
GPS_EPSILON_KM          0.3           격자 300m 에서 오는 바닥 오차
R_MIN_KM / R_MAX_KM     0.4 / 8       반경이 0 으로 수렴하거나 시 전체로 퍼지는 것 제한
SPOT_LIMIT              4             탐색 지점 개수
TRACK_MODEL             claude-sonnet-5
TRACK_PROMPT_VERSION    track-1
TRACK_PHOTO_MAX         4             최근 노드부터 첫 사진 한 장씩
```

공식마다 기댄 근거입니다.

```
T3  straightness index   이동생태학의 경로 직선성 지표, 합벡터 길이를 다리 길이 합으로 나눔
T4  평균제곱변위          등방 확산에서 변위가 √t 에 비례해 반경을 σ × √h 로 둠
T5  Rayleigh 최빈 거리    2차원 등방 확산의 반경 분포 최빈값이 σ 라 경계 부근이 밀도 최대
```

## 데이터와 안전

새 마이그레이션 없이 `reports` 와 `match_scores` 를 읽기만 합니다. 좌표는 `coarsePoint` 즉 300m 격자로 넓힌 공개용 값만 쓰고 `exactPoint` 를 읽는 코드는 0줄입니다. 노드 좌표는 지도에 이미 낱개로 떠 있는 지점들이라 경로가 새로 드러내는 위치 정보가 없습니다.

공개된 실종 신고는 관리 세션 없이 경로를 볼 수 있습니다. 어디를 찾아봐야 하는지 아는 사람이 많을수록 다시 만날 확률이 올라가고, 실종 신고는 보호자가 널리 퍼지기를 바라는 글이기 때문입니다. 숨겼거나 끝난 신고, 그리고 후보 덱과 연락 경로는 POL-03 대로 작성자 전용으로 남습니다.

## 실측 결과

판교역 북쪽에서 남동쪽으로 이동하는 시연 데이터 기준입니다.

```
노드           4개
경로 신뢰도     81
예측 반경       1.03km
직선성          1.000
방위            135도 (남동)
조언            stale 27.2시간 · 반경 1.2km 안 발견 제보 6건, 그중 닮은 후보 4건 · 커버리지 active
탐색 지점       통도골천 · 당골공원 · 태현공원 · 점골어린이공원   Kakao 실호출
해석            백현동에서 정자동 거쳐 금곡동으로 남동쪽 이동      Anthropic 실호출
순서            금곡동 일대 → 금곡동 남동쪽 인근 → 정자동과 금곡동 경계
응답 exactPoint 0건
```

같은 시연 데이터에서 탐색 대상 면적입니다.

```
가정                   반경      면적
v_max × h 순진한 원     20.4km   1307km²
고정 2km 원            2km      12.6km²
T4 예측 원             1.03km   3.3km²
조건                   소형, 마지막 목격 6.8시간 전, 직선성 1.000
```

순수 확산에 방향 지속을 더한 가정 아래의 계산값입니다. 면적이 줄어드는 것이 재회로 이어지는지는 측정하지 않았습니다.

## 탐색 조언

시간과 위치와 제보 밀도를 서버가 한 번에 읽어 지금 할 일 한 줄과 그 근거 한 줄로 바꿉니다. 모델이 아니라 규칙이 만드는 문장이라 화면에 AI 초안 표기를 붙이지 않고, 모델에는 규칙이 센 숫자만 넘겨 해석이 같은 사실 위에 서게 합니다.

```
기준 시각   보호자가 적은 실종 시각 하나. 후보 제보 시각은 개체가 확정되지 않아 기준으로 쓰지 않음
단계        fresh <6h · recent <24h · stale <72h · cold >=72h
행동        단계 × 동물로 고름. 고양이는 가까운 곳 반복 탐색, 개는 반경 확장
근거        반경 r km 안 발견 제보 n건(분모), 그중 닮은 후보 m건(분자)
커버리지    최근 7일 반경 3km 발견 제보 3건 미만이면 보는 눈이 적은 곳으로 보고 공유를 권함
반경 하한   격자 두 배. 300m 격자면 0.6km, 1000m 격자면 2km. 스냅 오차보다 작은 반경은 빈 문장
```

제보가 없는 것과 제보는 있는데 후보가 없는 것은 다음 행동이 다릅니다. 앞은 공유로 눈을 늘려야 하고 뒤는 지역을 옮겨야 합니다. 분모 없이 후보 수만 말하면 이 둘을 가를 수 없어 두 수를 함께 냅니다.

경계 시간 6·24·72와 커버리지 하한은 회수 데이터가 없어 정한 경험 규칙입니다. 단계가 응답에 남으므로 어떤 단계를 본 보호자가 무엇을 했는지 셀 수 있고, 그 데이터로 경계를 보정하는 것이 다음 일입니다. 제출 자료에는 데이터 기반이 아니라 규칙 기반, 보정 예정으로 적습니다.

## 함께 반영한 UX

```
할 일 → 근거 → 갈 곳   경로 카드는 세 덩이만. 지도와 타임라인이 이미 보여 주는 지역명과 시각은 되풀이하지 않음
시간 → 행동     fresh <6h · recent <24h · stale <72h · cold >=72h 네 등급에 다음 행동 한 줄
밀도 문구       반경 r km 안에 제보 n건, 0건이면 새 제보가 없어요
상태 어휘       실종 · 발견 · 보호 중 · 구조 요청 · 찾음 다섯 개만, unknown 은 빈 문자열
              배회 중 과 찾는 중 은 사용자 문구에서 빠짐. 단일 원천은 STATUS_LABEL
CTA            구조 요청 → 구조·보호 요청, 내 가족 같아요 → 우리 아이인지 확인
검색 분리       발견 제보 찾기 / 실종 신고 찾기, 실종은 반려동물 이름으로도 검색
```

사용자가 알고 싶은 것은 날짜가 아니라 얼마나 오래됐는지이고, 그다음은 그래서 무엇을 하면 되는지입니다. 시간을 원시값이 아니라 맥락과 행동으로 바꿉니다.

## 구현 위치

```
packages/core/src/matching/track.ts          T1~T4 순수 함수와 상수
packages/core/src/matching/search-spots.ts   T5 배점과 Kakao 호출 래퍼
packages/core/src/matching/track-review.ts   모델 해석과 사진 대조
packages/core/src/matching/track-handlers.ts GET /api/lost/:id/track
packages/db/src/queries/lost.ts              findTrackSightings
apps/web/components/lost/track-map.tsx       경로선 · 예측 원 · 번호 마커
packages/core/src/matching/search-advice.ts  탐색 단계·행동 문구·밀도·커버리지 순수 함수
packages/db/src/queries/lost.ts              countSightingsAround 분모 집계
apps/web/components/lost/track-section.tsx   할 일 · 근거 · 갈 곳 카드와 탐색 순서
apps/web/lib/report-label.ts                 긴급도 등급과 밀도 문구, 상태 어휘 단일 원천
apps/web/app/r/[id]/poster/page.tsx          A4 전단과 제보 QR, 색인 제외
apps/web/app/find/page.tsx                   검색 유입 랜딩
```

회귀 검증은 `track.test.ts`, `search-spots.test.ts`, `track-review.test.ts`, `search-advice.test.ts` 입니다. 패키지 전체는 `@rebirth/core` 133건, `@rebirth/web` 23건, `@rebirth/types` 6건이고 모두 `fail 0` 입니다.

## 한계

경로는 단일 사슬 하나만 만들고 갈라지는 경로를 다루지 않습니다. 비슷한 시각에 서로 다른 방향으로 남은 두 목격 중 하나만 사슬에 들어갑니다.

`buildTrack` 은 시간순으로 훑으며 이동 가능한 노드를 앞에서부터 담는 탐욕 선택이라, 앞 노드가 틀리면 그 뒤 판정이 전부 밀립니다.

사진 대조는 `photoConsistency` 가 `mixed` 일 때 `PHOTO_MIXED_FACTOR` 로 신뢰도를 깎는 감쇠뿐이라, 두 개체를 한 경로로 합칠 수 있습니다. 경로는 확인할 후보이고 개체 동일성을 확정하지 않습니다.

`scoreMatch` 는 실종 지점과 실종 시각을 기준으로 배점하므로 15km 14일 창 밖의 제보는 외형 유사도 `MIN_LEG_SIMILARITY` 로만 경로에 들어옵니다.

`MIN_LEG_SIMILARITY = 0.82` 는 실측에서 승격 0건입니다. 유사도 분포는 `scored=true` n=8 중위 `0.5839`, `scored=false` n=5572 상위 10% `0.6140`, 전체 5580쌍 최대 `0.778` 이고 두 분위수가 역전돼 표본으로 하한을 정할 수 없어 기본값을 그대로 두었습니다.

## 공식 심사 기준

```
예선   기획력 · 실현 가능성 · 확장성 · AI 활용의 적절성    원티드랩 내부 심사 80% + 온라인 투표 20%
본선   기획력 · 확장성 · 기술력 · 발표 전달력              AI 전문가 심사위원단
배점   항목별 가중치 배점 미공개                          보도자료와 공고 어디에도 항목 가중치가 없음
일정   제출 2026-09-20 · 예선과 투표 2026-09-21 부터 2026-10-05 · TOP20 2026-10-07 · 데모데이 2026-10-17
실격   심사 기간에 서비스 링크가 멎으면 평가에서 제외될 수 있음
출처   https://www.venturesquare.net/1109725 · https://www.m-i.kr/news/articleView.html?idxno=1407688 · https://zdnet.co.kr/view/?no=20260831083536
```

항목별 배점은 공개되지 않아 가중치를 가정하지 않습니다. 여덟 기준을 같은 무게로 두고 각각에 한 줄씩 답을 답니다.

## 예선 4기준에 대한 답

```
기획력          목격자는 사진 한 장을 3분 안에 넘기고 싶고, 보호자는 지금 어디를 찾아야 하는지 알고 싶습니다
                게시판은 글을 쌓지만 다시집은 흩어진 목격을 이어 이동 데이터로 바꿉니다
실현 가능성      https://re-birth.kr 로 이미 배포했고, 사진 분석 평균 6.3초와 탐색 지점 4곳이 Anthropic · Kakao Local 실호출 실측값입니다
확장성          T1 부터 T5 는 좌표와 시각만 받는 결정식이라 실종과 추적이 있는 어느 서비스에도 그대로 붙습니다
                UI 단일 원천을 당근 SEED 하나로 둬서 화면이 늘어도 토큰과 컴포넌트가 갈라지지 않습니다
AI 활용의 적절성  사람의 눈으로 하기 어려운 자리에만 모델을 씁니다
                좌표 계산과 개체 동일성 판정은 모델에 맡기지 않고 결정식과 사람의 확인으로 남깁니다
```

## 본선 4기준에 대한 답

```
기획력       예선 답과 같은 자리를 가리킵니다
확장성       예선 답과 같은 자리를 가리킵니다
기술력       T1 부터 T5 결정식 5개가 점수와 좌표를 만들고 모델은 계산값 해석과 사진 대조만 맡습니다
             pgvector 외형 유사도로 점수 창 밖의 제보를 경로에 올리는 길을 열어 두었습니다
             좌표는 300m 격자로 넓힌 값만 쓰고 exactPoint 를 읽는 코드가 0줄입니다
발표 전달력   아래 다섯 단계 대본으로 목격자 제보부터 보호자 경로 화면과 확산까지 165초에 보여 줍니다
```

## 심사위원 예상 질문

```
강정구  라이너 AI 전략 총괄, 라인맨과 타파스 스케일업 이력
        질문  어느 시점에 시장으로 들어가고 어떻게 넓히나
        답    발견 직후 3분 안이 진입 시점입니다. 전단 QR 과 공유 카드가 오프라인 전단과 메신저를 유입 경로로 씁니다
김호민  스파크랩 공동대표, 선발 요건이 타겟 고객에게 MVP 를 팔아 본 경험
        질문  실제로 쓰는 사람이 있나
        답    배포본 실사용 수치를 그 자리에서 엽니다. 집계 도구를 아직 붙이지 않아 수치가 미측정이고 제출 전에 붙이는 것이 남은 일입니다
김덕중  퍼브 AI 연구소장, 강연 제목 AI 의 진짜 효력은 기존 애플리케이션 사이에서 발생한다
        질문  모델을 어디에 끼워 넣었나
        답    제보 → 매칭 → 경로 → 공유 네 단계를 한 줄로 잇고 모델은 단계 사이의 빈자리만 채웁니다
조정석  크래프톤 AI 에이전트 엔지니어, 개인 이력 미확인
        질문  에이전트를 어떻게 설계했나
        답    에이전트 루프를 쓰지 않습니다. 결정식이 값을 만들고 모델은 한 번의 호출로 해석만 합니다
정기수  원티드랩 AI부문장
        질문  어떤 도구를 어떻게 썼나
        답    코드는 Claude Code, 런타임 추론은 claude-sonnet-5, 외형 벡터는 openai/text-embedding-3-small 로 갈라 씁니다
```

## 제출 문안 초안

제출 폼 다섯 칸에 그대로 넣는 값입니다.

```
대표 이미지            HERO-hero-16x9.png, 새 커버 문구와 새 캡처로 다시 굽는 것이 남은 일
제목                   다시집 Rebirth
해결하고자 한 문제      길에서 동물을 발견한 사람은 어디에 알려야 할지 모르고, 실종 신고를 낸 보호자는 지금 어디를 찾아야 할지 모릅니다. 다시집은 사진 한 장을 3분 만에 발견동물 제보로 바꾸고, 흩어진 목격을 이어 다음에 나타날 지역을 좁힙니다.
사용 AI툴 및 기술 스택   추론 claude-sonnet-5 · 임베딩 openai/text-embedding-3-small · 장소 Kakao Local
                       Next.js 16 App Router · React 19 · TypeScript · 당근 SEED · shadcn/ui · Supabase Postgres(PostGIS, pgvector) · Supabase Storage · Drizzle ORM · Vercel
                       코딩 도구 Claude Code
서비스 접속 링크         https://re-birth.kr
시연용 실종 신고         https://re-birth.kr/r/457847fe-4042-4419-9740-fdf10f0e6818
                       주소에 좌표가 없고 응답 본문에 exactPoint 0건, 좌표는 300m 격자만
```

`AI 활용 방식 및 결과` 는 500자 상한이라 아래 초안을 472자로 맞췄습니다. 현재 등록본에는 이동 경로 추적이 한 줄도 없어 이 초안으로 바꿉니다.

```
길에서 마주치는 동물은 대개 등록이 없어 사진 한 장에서 시작합니다. Claude Sonnet 이 사진에서 종류·크기·털색·특징을 읽어 제보 초안을 채웁니다. 평균 6.3초이고 제보자는 고칠 곳만 손봅니다. 품종은 말티즈 계열 추정처럼 계열로만 적습니다. 공공 공고 200건 라벨과 대조해 축종 99.5%, 털색 100% 를 확인했습니다.

실종 신고와 발견 제보는 거리 35·시간 25·털색 20·크기 10·특징 10 으로 채점하고 pgvector 외형 벡터를 더해 확인할 후보로만 내놓습니다. 같은 개체라고 확정하지 않습니다.

흩어진 목격을 시간순으로 이어 이동 경로를 만들고 다음 목격 지역과 탐색 순서를 냅니다. 방향성·표류·반경은 결정식이 계산하고 모델은 그 값을 행동 문장으로 바꿉니다. 시연 데이터에서 탐색 면적이 1307km² 에서 3.3km² 로 줄었습니다. 좌표는 300m 격자만 쓰고 프롬프트에 좌표 숫자를 넣지 않습니다.
```

## 시연 대본

```
1  목격자 제보 한 장          25초   사진 한 장을 올리면 AI 초안이 채워지고 고칠 곳만 손봐 등록합니다
2  확인할 후보                30초   실종 신고와 대조한 점수와 맞은 항목을 열어 확인할 후보로 봅니다
3  이동 경로                  40초   목격 4건이 시간순 선으로 이어지고 직선성과 방위가 함께 붙습니다
4  보호자 경로 화면            45초   예측 원 두 겹과 탐색 지점 네 곳이 뜨고 어디부터 찾을지 한 줄로 나옵니다
5  공유 카드와 전단 QR         25초   스토리 카드 저장과 A4 전단 출력으로 오프라인까지 넓힙니다
합계                        165초
```

## 남은 것

실종 신고가 없는 떠돌이 개체의 경로는 범위 밖입니다. 발견 제보끼리 배점하는 로직이 새로 필요합니다. 노출 빈도 관점에서 값어치가 있어 별도 건으로 잡는 것이 맞습니다.
